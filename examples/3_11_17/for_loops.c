#include <stdio.h>

int main()
{
	// c++ is equivalent to c = c + 1
	/*
	for (initialization; condition; incrementation(?))
	{
		body of the for-loop
	}
	*/
	for (int c = 0; c < 100; c++)
	{
		printf("c is %d\n", c); 
	}
	return 0; 
}
